[project]
name = "debugger-mcp"
version = "0.1.0"
description = "MCP server exposing DAP-based debugging tools for AI clients"
requires-python = ">=3.11"
dependencies = [
    "top-coder-ai-skills-debugger",
    "mcp",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
top-coder-ai-skills-debugger = { workspace = true }
